# Fixes OpenNext/wrangler static analysis on pnpm's symlinked node_modules,
# which otherwise causes "Dynamic require of *-manifest.json is not supported" (all routes 500).
# This layout is also safe for Vercel and VPS deploys.
node-linker=hoisted
shamefully-hoist=true
